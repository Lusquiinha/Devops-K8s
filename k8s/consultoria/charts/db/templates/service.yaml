apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db
  labels:
    app: {{ .Release.Name }}-db
spec:
  ports:
    - port: {{ .Values.global.postgres.port }}
      targetPort: {{ .Values.global.postgres.port }}
      protocol: TCP
  selector:
    app: {{ .Release.Name }}-db
